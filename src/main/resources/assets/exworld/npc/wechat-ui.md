# 都市 NPC 界面 · 微信风格

- 页面底 `#F7F7F7`，卡片与输入 `#FFFFFF`，次级底 `#EDEDED`，分割线 `#E5E5E5`。
- 主色 `#07C160`，可点次级文字 `#576B95`，危险 `#FA5151`。
- 主文字 `#191919`，说明 `#8A8A8A`。字号沿用游戏字体，标题不描边。
- 间距 8 的倍数。按钮高 20–24，导航宽 80，圆角用扁平色块近似。
- 图标用单色色块和首字，不使用外部贴图。
- `renderBackground` 保持空实现，不调用 `super`。
